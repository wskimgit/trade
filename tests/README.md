# Tests

Use this directory for reproducible regression tests and synthetic fixtures.

- `tests/fixtures/`: synthetic/redacted runtime examples only
- no live account/order/position state
- keep tests deterministic when possible
